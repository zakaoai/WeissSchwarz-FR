import ISiteMap from "@/interfaces/route/SiteMap"

const SiteMap: ISiteMap = {
  ACCUEIL: {
    path: "/app/home",
    label: "Accueil"
  },
  REGLE: {
    path: "/app/regle",
    label: "Règles"
  }
}

export default SiteMap
